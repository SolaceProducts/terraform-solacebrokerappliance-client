version: 3

project:
  locator: SolaceProducts_terraform-solacebrokerappliance-client
  id: SolaceProducts_terraform-solacebrokerappliance-client
  name: terraform-solacebrokerappliance-client
  teams: []
  labels:
    - terraform

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
